# Minimal .dockerignore - only exclude what we don't need
# CRITICAL: Do NOT exclude public/, src/, config/ - they are essential!
.git
.gitignore
*.md
!README.md
tests/
features/
docs/
vendor/
var/
.env.local
.env.*.local
node_modules/
# DO NOT exclude these - they are needed:
# public/, src/, config/, templates/, assets/, bin/, translations/
# If we exclude them, the application won't work!