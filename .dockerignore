# Minimal .dockerignore - only exclude what we don't need
# Don't exclude everything - let Docker copy what it needs
# Only exclude specific patterns that are definitely not needed
.git
.gitignore
*.md
!README.md
tests/
features/
docs/
vendor/
var/
.env.local
.env.*.local
node_modules/
# Explicitly include essential directories to ensure they're not excluded
!public/
!public/**
!src/
!src/**
!config/
!config/**
!templates/
!templates/**
!assets/
!assets/**
!bin/
!bin/**
!translations/
!translations/**